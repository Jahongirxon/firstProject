
import Navbar from "../navbar";

function Header() {
  return (
    <header className=" w-full bg-navbar h-[100px] flex justify-center items-center">
      <Navbar/>
    </header>
  );
}

export default Header;
